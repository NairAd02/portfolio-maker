export interface Blog {
  id: string;
  name: string;
  description: string;
  date: string;
  link?: string;
}

export interface BlogDetails {
  id: string;
  name: string;
  description: string;
  date: string;
  link?: string;
}
